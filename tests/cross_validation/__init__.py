"""Cross-validation tests against PyTorch.

These tests compare MLX Primitives implementations against PyTorch
to ensure correctness beyond internal NumPy reference implementations.

Run these tests with: pytest -m cross_validation
Requires PyTorch: pip install torch
"""
